1. Joins 很困难 通过Denormalization进行数据冗余处理,可以使得需要join的数据都在单一服务器上,但是这就带来的数据一致性的问题.
2. 外键的完整性几乎无法实现,因为不在一个db instance里面
3. 数据倾斜(容量) 和数据热点(性能) 通过一个dict进行对应关系的维护可以使得在这种情况下进行数据rebalance成为可能,
   但是这个查询对应表就成为了一个单点故障的可能.
4.
