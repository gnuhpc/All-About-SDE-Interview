前缀和

一般用在计算区间和

首先要声明前缀和
int preSum[] = new int[nums.length+1];
preSum[0] = 0;

然后计算前缀和
for (int i = 1; i <= nums.length; i++) {
    preSum[i] = preSum[i-1] + nums[i-1];
}

则区间和[i,k] = preSum[k-1] - preSum[i];

LC相关题目：
53 解法2

